import React from 'react';
import { CalendarType } from 'tui-date-picker';
export interface TuiDatePickerProps {
    handleChange: (e: any) => void;
    date?: Date;
    inputWidth?: number | 'auto';
    containerWidth?: number;
    containerHeight?: number;
    fontSize?: number;
    backgroundColor?: string;
    format?: string;
    timePicker?: boolean;
    dateType?: CalendarType;
    padding?: number;
    margin?: number;
}
declare const DatePickerComponent: ({ handleChange, date, inputWidth, containerWidth, containerHeight, fontSize, backgroundColor, format, timePicker, dateType, padding, margin, }: TuiDatePickerProps) => React.JSX.Element;
export default DatePickerComponent;
